using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//this is a patch for now will fix or change later in the future but for now just chill with it
//i'm too lazy to fix it so... whoever build the map better be smart with their item place ment lol
namespace ProjectL.Globals.Script.interaction
{
    public class InteractionScanner : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}
